/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "flowbite.com",
      "flowbite.s3.amazonaws.com",
      "tailwindui.com",
      "booking.ksbgarage.com",
    ],
  },
};

export default nextConfig;
